Packo::Features::Default.define(:ruby) {
  description 'Adds support/bindings for the Ruby language'

  on :dependencies do |package|
    package.dependencies << 'development/interpreters/ruby' if enabled?
  end
}
